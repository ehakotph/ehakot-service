import createBaseRouter from '../../utilities/base-router';
import Barangay from '@/models/public/barangay.model';

const barangayRouter = createBaseRouter(Barangay, {
});

export default barangayRouter;
